# Deprecated category editor components

These files were superseded by the unified single-surface category editor
(`CategoryEditorDialog` + `CategoryCopyFromLayerButton` + `CategoryCsvActions`).

Originals lived at:
- `src/components/form/CategoryEditorTabs.tsx`
- `src/components/form/CategoryCopyFromLayer.tsx`

Kept here for reference per the project's deprecation guideline. Do not import.
